import { HideElDirective } from './hide-el.directive';

describe('HideElDirective', () => {
  it('should create an instance', () => {
    const directive = new HideElDirective();
    expect(directive).toBeTruthy();
  });
});
